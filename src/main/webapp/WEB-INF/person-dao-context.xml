<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:elasticsearch="http://www.pilato.fr/schema/elasticsearch"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd http://www.pilato.fr/schema/elasticsearch http://www.pilato.fr/schema/elasticsearch/elasticsearch-0.3.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <property name="defaultAutoCommit" value="true" />
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <context:property-placeholder location="classpath:jdbc.properties"/>
    
    <!-- Override xml session factory imported from shared-context.xml -->
    <bean id="sessionFactory" 
          class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean"
          p:dataSource-ref="dataSource">
        <property name="packagesToScan" value="fr.pilato.demo.legacysearch.domain" />
        <property name="hibernateProperties">
            <value>
                hibernate.format_sql=true
                hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
                hibernate.hbm2ddl.auto=create
            </value>
        </property>
    </bean>
    
    <bean id="personDao" 
          class="fr.pilato.demo.legacysearch.dao.PersonDaoImpl"
          p:sessionFactory-ref="sessionFactory" />

    <bean id="searchDao"
          class="fr.pilato.demo.legacysearch.dao.SearchDaoImpl"
          p:sessionFactory-ref="sessionFactory" />

    <!-- TRANSACTION MANAGER -->
    <bean id="transactionManager" class="org.springframework.orm.hibernate3.HibernateTransactionManager"
            p:sessionFactory-ref="sessionFactory" />

    <!-- Jackson Mapper -->
    <bean id="mapper" class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <!--  Elasticsearch -->
    <util:properties id="esproperties"
                     location="classpath:elasticsearch.properties"/>

    <elasticsearch:client id="esClient" esNodes="127.0.0.1:9300"
                          properties="esproperties"/>

</beans>
